<UserControl x:Class="WallpaperTurbo.UI.Views.LayoutHostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:WallpaperTurbo.UI.Views"
             xmlns:minimal="clr-namespace:WallpaperTurbo.UI.Views.Minimal"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <ContentControl>
        <ContentControl.Resources>
            <DataTemplate x:Key="MinimalLayoutTemplate">
                <minimal:MinimalLayoutView />
            </DataTemplate>
            <DataTemplate x:Key="TechieLayoutTemplate">
                <views:TechieLayoutView />
            </DataTemplate>
        </ContentControl.Resources>
        <ContentControl.Style>
            <Style TargetType="ContentControl">
                <Setter Property="Content" Value="{Binding}" />
                <Setter Property="ContentTemplate" Value="{StaticResource MinimalLayoutTemplate}" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding LayoutHost.CurrentLayout}" Value="Techie">
                        <Setter Property="ContentTemplate" Value="{StaticResource TechieLayoutTemplate}" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ContentControl.Style>
    </ContentControl>
</UserControl>