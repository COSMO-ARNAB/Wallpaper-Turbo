<UserControl x:Class="WallpaperTurbo.UI.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="24">
        <StackPanel>
            <TextBlock Text="Wallpaper Library Manager" Style="{StaticResource HeaderLargeStyle}" Margin="0,0,0,16" />
            <TextBlock Text="Browse your entire collection or import new cinematic streams." FontSize="12" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,24" />
            
            <Border Style="{StaticResource NeonCardStyle}" Padding="24" HorizontalAlignment="Left" Width="400">
                <StackPanel>
                    <TextBlock Text="Quick Actions" FontSize="14" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,12" />
                    <ui:Button Content="+ Import Local Video" Style="{StaticResource NeonImportButtonStyle}" Margin="0,0,0,8" />
                    <ui:Button Content="Open Manifest File" Appearance="Secondary" Icon="Folder24" Margin="0,0,0,8" />
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
